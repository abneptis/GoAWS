SQS:
  - (highest) Message deletion
  - (high) clean up AWS errors
  - (med) fix spurious crashes on AWS errors (recent fmt on Nil *http.URL?)
  - (med) add tests
  - (med) add SSL support
  - (low) add ACL support

S3:
  - all

Fps/Cobranded:
  - all

Fps:
  - all

Ec2:
  - all

All:
  - Refactor some of the general auth/message structures as other types are
    needed.
